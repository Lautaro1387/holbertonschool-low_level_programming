#include "main.h"
#include <ctype.h>
#include <stdlib.h>
/**
 * _abs - Prints the sign of a number.
 * @n: The check.
 * Return: Anything.
 */
int _abs(int n)
{
_putchar = abs(n);
_putchar(n);
return (0);
}
