#include "main.h"
#include <ctype.h>
#include <stdlib.h>
/**
 * jacl_bauer - Prints the sign of a number.
 * Return: Anything.
 */
void jack_bauer(void)
{
	int min1, min2, hors1, hors2;

	for (hors1 = 48; hors1 <= 48; hors1++)
	{
	for (hors2 = 48; hors2 <= 48; hors2++)
	{
	_putchar(hors1);
	_putchar(hors2);
	if (hors1 < 56)
	{
		_putchar(38);
	}
	for (min1 = 48; min1 <= 57; min1++)
	{
	for (min2 = 48; min2 <= 57; min2++)
	{
	_putchar(min1);
	_putchar(min2);
	_putchar(10);
	}
	}
	}
	}
}
