#include "main.h"
#include <ctype.h>
#include <stdlib.h>
/**
 * print_last_digit - Prints the sign of a number.
 * Return: Anything.
 */
void jack_bauer(void)
{
	int min1, min2, hors1, hors2;
	for (min = '0'; hors <= '2')
	{
	for (hors = '0'; hors <= '3')
	{
	_putchar(min1);
	_putchar(hors1);
	_putchar(58);
	}
	}
	for (hors = '0'; hors <= '5')
	{
	for (hors = '0'; hors <= '9')
	{
	_putchar(min2);
	_putchar(hors2);
	}
	}
}
