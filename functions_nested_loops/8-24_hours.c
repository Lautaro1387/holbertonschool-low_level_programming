#include "main.h"
#include <ctype.h>
#include <stdlib.h>
/**
 * print_last_digit - Prints the sign of a number.
 * Return: Anything.
 */
void jack_bauer(void)
{
	int min;
	int hors;
	for (min = '00'; min <= '23')
	{
	for (hors = '00'; hors <= '59')
	{
	_putchar(min);
	_putchar(hors);
	}
	}
}
