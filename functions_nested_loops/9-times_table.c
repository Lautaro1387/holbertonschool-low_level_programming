#include "main.h"
#include <ctype.h>
#include <stdlib.h>
/**
 * times_table - Prints the sign of a number.
 * Return: Anything.
 */
void times_table(void)
{
	int a, b;
	int c = a * 9;
	int d = b * 9;
	for (a = c; a < 10; a++)
	{
	for (b = d; b < 10; a++)
	{
	_putchar(a);
	_putchar(b);
	_putchar(',');
	_putchar(10);
	_putchar(10);
	}
	}
}
