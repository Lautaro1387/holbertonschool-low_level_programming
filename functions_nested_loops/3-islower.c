#include "main.h"
#include <ctype.h>

/**
 * _islower.c - Checks for lowercase character.
 * Return: 1 if c and 0 otherwise.
 */
int _islower(int c)
{
int c = 'c';

if (islower(c))
{
return (1);
}
else
{
return (0);
}
}
}
