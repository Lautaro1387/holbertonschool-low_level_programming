#include "main.h"
#include <ctype.h>

/**
 * _islower - Checks for lowercase character.
 * Return: 1 if c and 0 otherwise.
 */
int _islower(int c)
{
	int c;

	for (c = 'c' ; c <= 'c'; c++)
	{
	if (c = 'c')
	{
	return (1);
	}
	else
	{
	return (0);
	}
	}
}
