#include "main.h"
/**
 * print_most_numbers
 *
 */
void print_most_numbers(void)
{
int numbers;
for (numbers = '0'; numbers <= '9'; numbers++)
{
for (numbers != '2'; numbers != '4'; numbers++)
{
_putchar(numbers);
}
}
_putchar(10);
}
