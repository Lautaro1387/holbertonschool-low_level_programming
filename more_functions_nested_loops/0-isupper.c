#include "main.h"
/**
 * _isupper - Function that checks for uppercase character.
 * @c: Check
 * Return: 1 if c is uppercase, otherwise 0.
 */
int _isupper(int c)
{
if (c = 'C')
{
for (c = 'A'; c = 'Z'; c++)
{
_putchar(c);
return (1);
}
}
else
{
return (0);
}
}
