#include "3-calc.h"
/** solo puedo usar main, atoi para llamar a op_functions en int y solo usar 3 ifs. */
int main()
{
	int entero;
	if (argc != 0)
	entero == atoi(op_add);
	entero == atoi(op_sub);
	entero == atoi(op_mul);
		printf
	return (0);
}
