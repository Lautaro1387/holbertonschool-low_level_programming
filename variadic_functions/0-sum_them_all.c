#include "variadic_functions.h"
/**
 * sum_them_all - Function.
 * @n: Element.
 * Return: 0.
 */
int sum_them_all(const unsigned int n, ...)
{
	if (n)
	{
		const int a = &n;
		return (n);
	}
	if (!n)
	return (0);
	return (0);
}
