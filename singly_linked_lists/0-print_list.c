#include "lists.h"
/**
 * print_list - Function of type size.
 * @h: const.
 * Return: size_t.
 */
size_t print_list(const list_t *h)
{

if (*str == NULL)
{
	printf("[0] (nil)");
}
return (list_t->h);
}
